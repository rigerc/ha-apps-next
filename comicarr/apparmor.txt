#include <tunables/global>

profile comicarr flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>
  #include <abstractions/ssl_certs>

  # Comicarr must manage user-selected library files whose numeric owner can
  # differ from the container user. No host, device, or admin capability is
  # granted.
  capability dac_override,

  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
  network unix stream,
  network unix dgram,
  signal (send, receive) set=(term,int,hup,kill,cont),

  /bin/bash rix,
  /bin/dash rix,
  /bin/sh rix,
  /run.sh rix,
  /usr/bin/7z rix,
  /usr/bin/7za rix,
  /usr/bin/7zr rix,
  /usr/bin/env rix,
  /usr/bin/git rix,
  /usr/bin/install rix,
  /usr/bin/python3* rix,
  /usr/bin/unrar* rix,
  /usr/lib/git-core/** rix,
  /usr/local/bin/python* rix,

  /opt/comicarr/ rm,
  /opt/comicarr/** rm,

  /dev/null rw,
  /dev/random r,
  /dev/urandom r,
  /dev/shm/ rw,
  /dev/shm/** rwk,
  /etc/** r,
  /lib/** rm,
  /proc/** r,
  /sys/devices/system/cpu/online r,
  /tmp/ rwk,
  /tmp/** rwk,
  /usr/lib/** rm,
  /usr/local/lib/** rm,
  /usr/share/** r,

  /data/ rw,
  /data/options.json r,
  /data/comicarr/ rwkl,
  /data/comicarr/** rwkl,

  /media/ rw,
  /media/** rwkl,
  /share/ rw,
  /share/** rwkl,

  /run/ rwk,
  /run/** rwk,
}
